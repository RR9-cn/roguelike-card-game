# 决策过程记录 · 午夜落槌 · 藏品连锁

> 生成日期：2026-09-20
> PRD 来源：《午夜落槌 · 藏品连锁》从0到1产品需求文档（工作流节点「AI-Spec生成」任务描述）
> 对齐轮次：2 轮（第 1 轮 5 个决策点；第 2 轮 3 个确认点 + 编码授权；其余决策由 Agent 基于 PRD 与工程惯例推断为默认决策）
> 上游产物：无（`artifacts/_manifest.json` 为空，上游「需求澄清」节点未同步任何产物，无可继承决策）

---

## 任务拆分阶段决策

### 决策清单

| # | 决策维度 | 决策点 | AI 推荐答案 | 用户最终决策 | 决策依据 / 理由 |
|---|---|---|---|---|---|
| 1 | 需求范围 | 本期交付范围 | 全量交付（PRD §19 全部验收用例单期完成） | **采纳：全量交付** | PRD §20 明确"交付完成以第 19 节全部验收用例通过为准"；本需求为 0→1 原型，无存量兼容负担，无需分期 |
| 2 | 核心技术选型 | 前端技术栈 | TypeScript + Vite + React 18 + Vitest（无游戏引擎） | **采纳** | 规则内核为纯 TS 逻辑，无物理引擎/粒子需求；DOM + CSS 动效足以满足 PRD §14；Vitest 便于内核在 node 环境测试（PRD §18 要求内核不依赖 DOM） |
| 3 | 持久化 | 本地存档载体 | localStorage 单键 JSON + schemaVersion 迁移 | **采纳** | 数据量极小（单局 < 30 KB），单键原子写入天然满足"操作与存档同事务"；浏览器与桌面壳同构 |
| 4 | 数据模型产物 | 数据模型产物形态 | 不产出 SQL DDL，改产出《数据模型与持久化契约》 | **修正：仍产出 SQLite 建表 DDL（CREATE TABLE）** | 用户要求以 SQLite DDL 表达规范数据模型。（第 1 轮口径：运行时 localStorage + SQLite 规范模型；**该口径已由第 2 轮 U-07 修订**为"浏览器 localStorage / 桌面壳强制 SQLite"，见下方第 2 轮确认记录） |
| 5 | 交付形态 | PC 桌面离线壳 | Tauri v2（Rust 壳，体积小，可禁外部导航） | **采纳** | 直接加载 Vite 静态产物，满足 PRD §18"生产构建不依赖开发服务器""禁止未授权外部窗口与导航"，包体远小于 Electron |
| 6 | 核心链路 | 核心链路识别 | 开局 → 手牌/排列 → 保底预览 → 上拍结算（入账+落盘）→ 结算播放与继续 → 判定（下一轮/奖励→升级/通关/失败） | **AI 判断（未提问）** | 该链路承载全部核心卖点（顺序触发、连锁、未选牌保留、单局成长），PRD §6 流程图即为该链路 |
| 7 | 模块划分 | 模块边界 | 按业务能力垂直切分 6 个模块：规则内核、存档与持久化、本地 AI 接口、界面与交互、测试与验收、桌面离线壳 | **AI 判断（未提问）** | 内核与 UI 解耦是 PRD §18 硬约束，必须独立成模块；存档、AI 接口、桌面壳各自独立可并行 |
| 8 | 核心状态机 | 对局阶段状态机 | 6 个阶段：hand / settlement / reward_pick / upgrade_pick / run_won / run_lost | **AI 判断（未提问）** | 由 PRD §6 流程图、§11 奖励升级顺序、§12 胜负判定直接推导 |
| 9 | 页面/接口规模 | 规模共识 | 7 个页面 + 2 个弹窗 + 11 个本地 AI 接口（含 1 个 CLI 批量入口） | **AI 判断（未提问）** | 页面按 PRD §13 清单；接口按 PRD §17 操作清单（开始/观察/预览/出牌/撤换/继续/收集/升级/历史/关闭）+ 批量回归入口 |
| 10 | 模块依赖 | 交付顺序 | 内核与存档先行（建议同一负责人整体承接），AI 接口与界面并行，测试随行介入，桌面壳收尾 | **AI 判断（未提问）** | 存档字段即内核状态，二者强耦合；界面与 AI 接口均只依赖内核契约，可并行 |
| 11 | 外部协作依赖 | 外部阻塞 | 无（无第三方接口、无账号、无凭据、无运营配置） | **AI 判断（未提问）** | PRD §18 要求完全离线；仅桌面壳需要本地 Rust 工具链（可后置） |

### 待定项

| # | 决策维度 | 决策点 | 待定原因 | 后续跟进建议 |
|---|---|---|---|---|
| 1 | 规则语义 | 木槌读取额外触发计数的口径（PRD §8.3「立即加1」与 §9.2「读取本次触发前」两种读法） | PRD 文本存在张力；两种读法均可复现 PRD §8.4 三个示例，但 `木槌→镜子→镜子` 结果不同（54 vs 90） | **已关闭（U-06，2026-09-20）：采纳"不计入自身"（54）**，TC-006 锁定该口径 |
| 2 | 持久化 | 桌面壳是否强制以 SQLite 为唯一持久化载体 | 用户同时选择了 localStorage（U-03）与 SQLite DDL（U-04），需明确二者关系 | **已关闭（U-07，2026-09-20）：桌面壳强制 SQLite**；D-04 据此修订为"双载体分工"（浏览器 localStorage / 桌面壳 SQLite） |
| 3 | 内容范围 | 规则弹窗是否额外收录数值曲线说明 | PRD §13.1 只要求固定 7 项内容 | **已关闭（U-08，2026-09-20）：采纳默认不收录** |

### 状态机推演补全记录

> 以下为 PRD 未明确描述、但通过状态机与运行环境推演发现必须闭环的点。

| # | 所属状态机 | 状态转换路径 | 推演出的闭环点 | 接口类型 | 用户确认状态 |
|---|---|---|---|---|---|
| 1 | 存档状态机 | `save_valid → save_corrupted` | 损坏存档检测、原文转存（`mnh.save.corrupted.<时间戳>`）、允许新局、禁止白屏 | 内部处理 + 首页提示（D-10 之外的 FR-02/FR-03） | Agent 默认决策（成本低、PRD §15.3 已要求"不得白屏"） |
| 2 | 存档状态机 | `save_valid → save_valid`（并发写） | **多标签页并发写冲突**：写入前比对存储 `seq`，不一致拒绝并提示重载（乐观锁，不静默覆盖） | 内部处理 + `SAVE_CONFLICT` 错误码 | Agent 默认决策（D-10），已记录为风险 R-05 |
| 3 | 存档状态机 | `save_valid → save_valid`（写入失败） | 写入失败（配额/隐私模式）时回滚操作，保证内存与存档一致 | 内部处理 + `SAVE_WRITE_FAILED` | Agent 默认决策（D-08），已记录为风险 R-06 |
| 4 | 对局阶段状态机 | `settlement → hand/reward_pick/run_won/run_lost` | 结算与入账必须在同一份快照中提交，否则"刷新恢复"可能重复入账或丢流水 | 内部提交顺序约定（Spec §12.3） | Agent 默认决策（PRD §15.3 要求"不重新判定概率或重复入账"） |
| 5 | 对局阶段状态机 | `hand → settlement`（提交前） | 触发事件 128 上限在**预览阶段**即需检测，避免玩家提交必然失败的组合 | 内部处理 + `RULE_TRIGGER_LIMIT_EXCEEDED` | Agent 默认决策（D-09） |
| 6 | 存档状态机 | `save_valid → save_valid`（版本不一致） | 规则版本不一致时的继续策略（结构可解析则继续并提示） | 内部处理 + 顶栏提示 | Agent 默认决策（D-11） |
| 7 | 对局阶段状态机 | `hand → hand`（撤换与排列槽交叉） | 被撤换且位于排列槽的实牌必须从槽位移除，避免"槽位引用已弃牌"非法状态 | 内部处理 | Agent 默认决策（D-13） |

---

## 技术分析阶段决策

> 技术分析日期：2026-09-20
> 本次范围：全部 6 个模块（M1 规则内核、M2 存档与持久化、M3 本地 AI 接口、M4 界面与交互、M5 测试与验收、M6 桌面离线壳与打包）
> 对齐轮次：0 轮增量提问（全部决策已由任务拆分阶段对齐 + Agent 默认决策覆盖，无新增必须用户拍板项）

### 继承确认

| # | 上阶段决策维度 | 上阶段决策结论 | 本次继承状态 | 修正内容（若有） |
|---|---|---|---|---|
| 1 | 需求范围 | 全量交付（PRD §19 全部用例） | 继承 | 无 |
| 2 | 技术选型 | TypeScript + Vite + React 18 + Vitest | 继承 | 无 |
| 3 | 持久化 | localStorage 单键 JSON + schemaVersion 迁移 | 继承并细化 | 第 1 轮明确为"运行时 localStorage + SQLite 规范模型"（D-04）；**已由第 2 轮 U-07 修订**为双载体分工 |
| 4 | 数据模型产物 | SQLite 建表 DDL | 继承并细化 | 明确方言适配（SQLite 无 COMMENT/ENGINE/ON UPDATE 的等价落地）与 JSON↔SQLite 字段映射（`data-model.md` §6） |
| 5 | 桌面壳 | Tauri v2 | 继承 | 无 |
| 6 | 模块划分 | 6 个模块 | 继承 | 无 |
| 7 | 状态机 | 6 个阶段 + 存档状态机 | 继承 | 无 |

### 存量业务调研记录

**本次不涉及存量业务。** 目标仓库 `RR9-cn/roguelike-card-game`（commit `606b0c5`）仅含 `README.md`，无既有接口、无既有表结构、无既有配置、无既有技术栈约束；`knowledge/context/` 为空，无知识库证据可引用。因此本阶段全部设计基于 PRD 与工程惯例，不涉及存量兼容。

### 技术决策清单

| # | 决策维度 | 决策点 | AI 推荐答案 | 用户最终决策 | 决策依据 / 理由 |
|---|---|---|---|---|---|
| D-01 | 规则语义 | 额外触发计数与木槌读取时机 | 计数在额外触发派发时立即 +1；能力读取本次触发开始前的快照 → 木槌被额外触发时不计入自身 | Agent 默认决策（待确认 Q-01） | PRD §9.2 是针对木槌的具体规则（具体优先于一般）；该口径可逐位复现 PRD §8.4 与 §19.1 三个示例；改判仅影响 1 行快照位置 |
| D-02 | 架构 | 随机流数量与派生 | 三条独立流 `draw` / `reward` / `chance`，由 `hash(seed + "#<流名>")` 派生 | Agent 默认决策 | PRD §16 要求"抽牌、奖励和红手套概率使用可保存的确定性随机状态"且"抽牌随机与概率随机相互独立"；三条流保证预览/观察不扰动任何随机消费 |
| D-03 | 规则语义 | 保底口径 | 红手套一律视为未触发，其余能力照常计算 | Agent 默认决策 | PRD §10"预览不得判定红手套"；配合能力单调非负，该口径即同顺序最小值（附证明，Spec §11.6） |
| D-04 | 持久化 | localStorage 与 SQLite DDL 的关系 | **修订（U-07）：浏览器版 = localStorage 单键 JSON（唯一载体）；桌面壳 = SQLite 库文件（唯一载体，强制）**；两载体共享同一 `saveSchemaVersion`、迁移链与字段映射 | Agent 默认决策 → **用户修订（U-07）** | 载体差异被限制在 `src/platform/`；内核/AI/界面完全共用；以 TC-054 防分叉 |
| D-05 | 接口边界 | AI 会话与玩家存档的关系 | 默认独立（不写玩家存档键）；`persist: true` 才绑定 | Agent 默认决策 | 批量回归不得污染玩家进度；PRD §17 只要求"持久会话 ID"，未要求写存档 |
| D-06 | 前端架构 | 视图路由 | 单入口应用壳，视图由存档 `phase` 驱动 + hash 路由辅助 | Agent 默认决策 | 刷新恢复必须以存档为准（PRD §15.3）；URL 不作状态真相源 |
| D-07 | 交互 | 覆盖已有存档 | 开始新局前二次确认 | Agent 默认决策 | PRD 未描述；避免误删进行中对局 |
| D-08 | 一致性 | 存档写失败语义 | 回滚本次操作并提示，提供重试 | Agent 默认决策 | 与 REQ-015"被拒绝的操作不得改变状态"一致；避免内存/存档不一致 |
| D-09 | 可玩性 | 预览命中 128 上限 | 预览阶段即报错并禁用上拍 | Agent 默认决策 | 避免玩家提交必然失败的组合 |
| D-10 | 并发 | 多标签页并发写 | 写入前比对存储 `seq`，不一致拒绝并提示重载 | Agent 默认决策（推演补全） | localStorage 同源共享；静默覆盖会丢进度 |
| D-11 | 兼容 | 规则版本不一致 | 结构可解析则允许继续并提示版本变化 | Agent 默认决策 | 本地单机无灰度需求；禁止白屏 |
| D-12 | 兼容 | 小于 1000×700 的窗口 | 允许纵向滚动，核心操作区不被遮挡 | Agent 默认决策 | PRD §14 只约束 1000×700 及以上无横向滚动 |
| D-13 | 状态一致性 | 撤换与排列槽交互 | 撤换选择集独立；被撤换且位于槽位的牌从槽位移除并压缩 | Agent 默认决策（推演补全） | 避免"槽位引用已弃牌"的非法状态 |
| D-14 | 交互 | 短种子生成 | 6 位 `A-Z0-9`，由平台密码学随机源生成（不占用游戏随机流） | Agent 默认决策 | PRD §6.1 要求自动生成短种子但未定长度；不占用游戏流以保证确定性语义 |
| D-15 | 状态一致性 | 场开始时清理 | 全部实牌洗入抽牌堆，清空弃牌区与排列槽 | Agent 默认决策 | PRD §6.2"完整收藏重新洗牌" |

### 撰写中新发现决策点

| # | 发现位置 | 决策点 | 决策结论 | 决策依据 |
|---|---|---|---|---|
| 1 | Spec §11.6 结算算法 | 保底不变量的可证明性 | 全部能力效果单调非负（只增加热度或派发额外触发），因此"红手套全失败"即最小值，`price ≥ floorPrice` 可证 | 逐条核对 PRD §9 十二种能力，确认不存在负向效果或改变其他牌点数的能力 |
| 2 | Spec §12.3 提交模型 | 提交顺序 | 采用"校验 → 计算 → 自检 → 落盘（含回读校验）→ 提交内存"，而非"先改内存再异步落盘" | 崩溃一致性：最坏情况为操作未生效，不会出现半写入或内存/存档分叉 |
| 3 | Spec §6.2 / data-model §6.3 | 流水事件字段设计 | 事件统一携带 `totalPointsAfter` / `heatAfter` / `extraCountAfter`，使结算页与 AI `history` 可直接回放而无需重算 | 支撑 PRD §13.4"刷新后恢复相同完整结算"与 §17 公开结算返回 |
| 4 | Spec §16 | 可观测性载体 | 本地环形缓冲（200 条）+ 结算流水；不引入任何上报通道 | PRD §18 要求不采集个人信息、离线运行 |
| 5 | data-model §1.3 | DDL 方言适配 | 按 SQLite 方言落地，并逐项列出与 `ddl-conventions.md`（MySQL 风格）的等价替换 | 用户确认 U-04 要求 SQLite DDL；规范中的 MySQL 专属语法（ENGINE/COLLATE/UNSIGNED/ON UPDATE/COMMENT）无法在 SQLite 表达，必须显式说明替代方案而非静默偏离 |

### 多视角 Review 记录

| # | Review 视角 | 问题点 | 风险等级 | 建议处理方式 | 用户确认结果 |
|---|---|---|---|---|---|
| 1 | 架构师 | D-01（木槌计数口径）与 PRD §8.3/§9.2 的文本张力：若策划预期为"计入自身"，`木槌→镜子→镜子` 应为 90 而非 54 | 中 | 保留默认口径 + TC-006 锁定 + 编码前确认（Q-01） | 待用户确认（已记录为风险 R-01） |
| 2 | 架构师 | U-03 与 U-04 的组合存在两种解释（localStorage 唯一载体 vs SQLite 唯一载体），若不明确会导致桌面壳实现分叉 | 中 | 已按 D-04 统一口径并在 `data-model.md` 显式说明；改动收敛在适配器层 | 待用户确认（Q-02） |
| 3 | 架构师 | 多标签页并发写会静默丢失进度（PRD 未覆盖） | 中 | D-10 乐观锁 + `SAVE_CONFLICT` + 提示重载 | 已采纳（Agent 默认决策，记录为 R-05） |
| 4 | 架构师 | 数值曲线（150/260/420）在 4 轮内是否可达无法从 PRD 验证 | 中 | 目标与点数全部配置化，调参不改结构 | 已采纳（记录为 R-03） |
| 5 | 资深开发 | 存档写入失败若只提示不回滚，会出现"界面已入账、刷新后丢失"的不一致 | 中 | D-08 回滚 + `SAVE_WRITE_FAILED` + 重试入口 | 已采纳 |
| 6 | 资深开发 | 结算入账与落盘若分成两次写入，刷新可能重复入账 | 高 | Spec §12.3 提交模型：结算结果与入账在同一快照内提交 | 已采纳 |
| 7 | 资深开发 | 能力实现若写在调度器的 `switch` 中，后续新增模板需改核心代码 | 低 | `CARD_TEMPLATES` + `ABILITY_REGISTRY` 注册表（Spec §12.2） | 已采纳 |
| 8 | 资深开发 | AI 公开视图若直接序列化内部状态，会泄露抽牌堆顺序与随机状态 | 高 | 白名单投影 + TC-046 结构断言 | 已采纳 |
| 9 | 资深开发 | SQLite 方言与 `ddl-conventions.md` 强制项冲突 | 低 | `data-model.md` §1.3 逐项列明等价替换 | 已采纳 |
| 10 | 资深开发 | 红手套递归 + 额外触发可能出现无限/指数展开 | 低 | 128 事件上限 + 有界展开（能力只向左触发，链深有界）+ 性能基准 | 已采纳 |

### 待定项

| # | 决策维度 | 决策点 | 待定原因 | 后续跟进建议 |
|---|---|---|---|---|
| 1 | 规则语义 | 木槌读取额外触发计数是否计入自身这次额外触发（D-01） | PRD 两处表述存在张力，需产品/策划拍板 | **已关闭（U-06）：不计入自身（54）**；`countBefore` 快照位置与 TC-006 期望值锁定 |
| 2 | 持久化 | 桌面壳是否强制使用 SQLite 作为唯一持久化载体（D-04） | 用户同时选择了 localStorage 与 SQLite DDL，需明确优先级 | **已关闭（U-07）：强制 SQLite**；落地于 Spec §12.3.1/SEC-08、`data-model.md` §6.6、T-02-06（由可选升为必需）、TC-051–TC-054 |
| 3 | 内容 | 规则弹窗是否收录数值曲线说明 | PRD 未要求 | **已关闭（U-08）：不收录**；仅文案 |

---

## Spec 阶段用户确认（第 2 轮 Human In Loop，2026-09-20）

> 触发原因：Spec 定稿后向用户汇报 3 项待确认项与编码授权请求，用户一次性回复。

### 决策清单

| # | 决策维度 | 决策点 | AI 推荐答案 | 用户最终决策 | 落地位置与影响 |
|---|---|---|---|---|---|
| U-06 | 规则语义 | 木槌额外触发计数口径（Q-01） | 不计入自身 → `木槌→镜子→镜子` = 18 点 × 3 热度 = 54 | **采纳默认** | Spec §11.6 D-01 标注已确认、§19.1 U-06、TC-006 期望值 54；§11.6 判定表与附录 A 同步；无需改算法 |
| U-07 | 持久化 | 桌面壳持久化载体（Q-02） | 桌面壳默认沿用 localStorage，SQLite 为可选适配器 | **修正：桌面壳强制以 SQLite 为唯一载体** | 新增 Spec §12.3.1（SQLite 落盘契约）与 §13.2 SEC-08（库文件边界）；修订 §10.1、§12.1、§12.4/§12.5、§14、§15、§17.4、NG-008、D-04、R-02；`data-model.md` §1.2/§1.3/§1.4 A2 与新增 §6.6；T-02-06 由"可选"升为**必需**、T-06-02 增加 SQLite 接入；新增 TC-051–TC-054；`tech-analysis` §1/§1.4/§2.1/§3.5/§4.3/§6 同步 |
| U-08 | 内容范围 | 规则弹窗是否收录数值曲线说明（Q-03） | 不收录，仅 PRD §13.1 要求内容 | **采纳默认** | Spec §19.1 U-08、§19.4 Q-03 关闭；REQ-017 / TC-065 口径不变 |
| U-09 | 阶段流转 | 是否授权进入「AI-编码」 | — | **授权进入 AI-编码** | Spec 冻结为编码基线；本节点交付后由编码节点按 Spec 实施 |

### 待定项收敛结论

| 原编号 | 状态 | 说明 |
|---|---|---|
| Q-01 | **已关闭** | 采纳默认（U-06） |
| Q-02 | **已关闭** | 修正为桌面壳强制 SQLite（U-07） |
| Q-03 | **已关闭** | 采纳默认（U-08） |

> 截至 2026-09-20，Spec 无未决项、无待确认占位；全部默认决策（D-01–D-15）冻结，其中 D-01 经用户确认、D-04 经用户修订。

---

## Spec 阶段验证与修正记录

> 验证日期：2026-09-20（工作流节点「AI-Spec生成」）
> 验证方式：不依赖任何游戏代码，独立重写 Spec §11.6 结算算法与 `data-model.md` 第四章 DDL 并实际执行，用 PRD 给的确定值反向校验规范。

### 独立验证结果（全部通过）

| 验证对象 | 方法 | 结果 |
|---|---|---|
| PRD §8.4 / §19.1 三个示例（36 / 90 / 105） | 独立实现 §11.6 伪代码，逐位比对点数、热度、成交价与触发事件数 | 三例逐位一致（事件数 4 / 4 / 6，与附录 A 一致） |
| D-01 口径（木槌被额外触发时不计入自身） | 按"计入自身"的替代读法重算 `木槌→镜子→镜子` | 得 90，与文档所述 54 vs 90 的差异一致，D-01 锁定有效 |
| 保底不变量（GOAL-004） | 遍历 12 模板全部 1320 个有序三元组 + 20000 组随机点数/随机概率采样，比对 `price` 与 `floorPrice` | 违反 0 次，与 §11.6 单调性证明一致 |
| 触发事件上限可达性 | 统计全部排列的最大触发事件数 | 最大 6（能力只向左派发，链深有界），确认 128 为纯安全网 |
| 确定性复现 | 同种子同顺序重复计算 | 流水与成交价完全相同 |
| DDL 可执行性 | 在 SQLite 3.37 中逐条执行第四章 DDL、插入冒烟数据、验证唯一约束 | 见 `data-model.md` 附录 A |

### 验证中发现并修正的缺陷

| # | 缺陷 | 严重度 | 影响 | 修正与决策依据 | 用户确认 |
|---|---|---|---|---|---|
| V-01 | `data-model.md` 中 7 张表共用索引名 `idx_createtime`，SQLite 下建索引报错 `index idx_createtime already exists`，第四章 DDL 无法整体执行 | **高（阻断）** | 桌面壳 SQLite 适配器（T-02-06）无法按文档建表 | 索引名按表前缀化为 `idx_<表名>_createtime`。决策依据：U-04 明确要求 SQLite DDL，而 `ddl-conventions.md` 的 `idx_createtime` 命名隐含 MySQL「索引名表内唯一」语义；SQLite 索引名空间为库级全局唯一，二者冲突时以**可执行性**优先，并在 `data-model.md` §1.3 方言适配表逐项写明偏离原因（字段与索引列语义不变） | Agent 默认决策（不涉及产品语义，属方言落地细节） |
| V-02 | `data-model.md` §3.1 状态机将「第 3 场达标」指向 `4 = upgrade_pick`，与 PRD §12、Spec §7.2/§7.3、`tech-analysis` §4.4.1 矛盾；`6 = run_won` 无入边，第 3 场胜利无法进入通关页 | **高** | 若按该图实现，第 3 场胜利会错误进入奖励/升级流程，违反 PRD §12 与 §19.3.5 | 修正为 `2 → 6 = run_won`（直接通关、不发奖励），并补齐 `run_won` / `run_lost` 终态出边。决策依据：PRD §12 判定顺序 + PRD §11「第 1 和第 2 场胜利后」才发放奖励，三处文档统一 | Agent 默认决策（PRD 已明确，属文档一致性修正） |
| V-03 | §二 关系说明称 `mh_run_history` 通过 `seed + save_id` 关联，但该表无 `save_id` 字段 | 低 | 关系描述与 DDL 不符，易误导实现 | 改为"通过 `run_id` / `seed` 关联，不建物理外键" | Agent 默认决策 |
| V-04 | 软删除约定称"业务表统一 `is_del`"，但 `mh_save_line_slot`、`mh_save_reward_candidate`、`mh_save_settlement_event`、`mh_run_history`、`mh_card_template` 未带该字段 | 低 | 规范表述与 DDL 不一致 | 在 `data-model.md` §1.3 明确 `is_del` **适用范围**（可逻辑删除的业务表 / 整体重建的派生表 / 只插不改的追加型表 / 配置表），不为一致性而添加永不被写入的死字段 | Agent 默认决策 |
| V-05 | 追溯矩阵未引用已定义的 T-02-06（SQLite 适配器）与 T-03-02（会话注册表） | 低 | 任务与需求的可追溯性不完整 | 补入 REQ-014 与 REQ-016 行，矩阵覆盖全部 33 个任务 | Agent 默认决策 |
| V-06 | 接口标识跨产物不一致：`api-spec.md` / `tech-analysis` 用 `auction.ai.*`，Spec §8 用 `API-00x` + `operation`/`decision.type`，三份文档间无显式对照 | 低 | 编码时可能被理解为两套接口，或新增未登记接口 | 在 Spec 新增 §8.1.1「命名与编号对照」固定 11 个接口的三方对应关系（并声明 `auction.ai.*` 为接口标识而非 HTTP 路由），`api-spec.md` 增加指向该表的对照说明 | Agent 默认决策（不改契约，仅补对照） |

> 结论：以上 6 项均为**文档一致性/方言落地**修正，不改变任何已确认的产品规则、默认决策（D-01–D-15）与接口契约；修正后 Spec 与配套产物之间无已知矛盾。

> **第 2 轮确认后的复核（2026-09-20）**：U-07（桌面壳强制 SQLite）为**用户修订项**，已按上文"落地位置与影响"逐份同步 5 份产物；同步后重新校验：SQLite DDL 仍可整体执行（18 条语句 0 失败）、追溯矩阵覆盖 20/20 REQ、33/33 任务、新增 TC-051–TC-054 均已登记且被任务引用，现行表述中无残留将桌面壳描述为"可选适配器"的措辞（仅历史决策记录保留第 1 轮原推荐口径，并已就地标注"已由 U-07 修订"）。
